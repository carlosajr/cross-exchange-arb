# ======= Geral =======
SYMBOL=BTC/USDT
QUOTE_BUDGET=100        # USDT por trade (ex.: 100)
SPREAD_MIN_BP=8         # spread mínimo em basis points (8 = 0.08%)
SLIPPAGE_BUFFER_BP=5    # buffer de slippage (5 = 0.05%)
STALE_MS=1000           # dados mais antigos que isso são ignorados
DRY_RUN=true            # true = não envia ordens

# ======= Binance (Spot) =======
BINANCE_API_KEY=...
BINANCE_API_SECRET=...

# ======= OKX (Spot) =======
OKX_API_KEY=...
OKX_SECRET=...
OKX_PASSWORD=...        # passphrase da OKX

# (opcional) Ajuste de taxas se quiser ser conservador
FEE_TAKER_BP=10         # 10 = 0.10% por ponta (estimativa conservadora)
